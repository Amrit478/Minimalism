/*By requiring a valid JWT with every request to protected endpoints,
the system ensures that only authenticated and authorized users can access 
sensitive resources and perform specific actions, thereby establishing secure endpoints for the system.*/
